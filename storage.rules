rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all students and users to view profile images, lecture notes, and VIP receipts
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null || true;
    }
  }
}
