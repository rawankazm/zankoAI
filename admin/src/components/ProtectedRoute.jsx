import React from 'react';

// DIRECT ACCESS: Login requirement removed by developer request.
// Allows seamless direct entry to the admin dashboard.
export default function ProtectedRoute({ children }) {
  return children;
}
